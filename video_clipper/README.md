# Video Clipper — автоматический нарезчик коротких видео

Десктопное приложение для автоматической нарезки длинных видео (шоу, стримов, сериалов)
на короткие вертикальные клипы (YouTube Shorts / TikTok / Instagram Reels).

## Возможности

1. **Графический интерфейс** на CustomTkinter с тёмной темой.
2. **Извлечение аудиодорожки** из видео через MoviePy.
3. **Распознавание речи** локальной моделью OpenAI Whisper с таймкодами.
4. **Поиск хайповых моментов** по ключевым словам, смеху, аплодисментам, крикам.
5. **Автоматическая нарезка** в вертикальный формат 9:16 с эффектом размытия фона
   (для обхода фильтров уникальности YouTube).
6. **Наложение рекламного баннера** (PNG с прозрачностью) в верхнюю или нижнюю часть клипа.

## Структура проекта

```
video_clipper/
├── main.py                     # Точка входа в приложение
├── requirements.txt            # Зависимости
├── README.md                   # Этот файл
│
├── gui/                        # Графический интерфейс
│   ├── __init__.py
│   ├── app.py                  # Главное окно приложения
│   ├── theme.py                # Цветовая схема и стили
│   └── widgets.py              # Кастомные виджеты (прогресс-бар, поля)
│
├── core/                       # Логика обработки видео
│   ├── __init__.py
│   ├── audio_extractor.py      # Шаг 2: извлечение аудио (moviepy)
│   ├── speech_analyzer.py      # Шаг 3: транскрипция (whisper)
│   ├── hype_finder.py          # Шаг 4: поиск хайповых моментов
│   ├── video_cutter.py         # Шаг 5: нарезка 9:16 + blur
│   └── banner_overlay.py       # Шаг 6: наложение баннера
│
├── utils/                      # Утилиты
│   ├── __init__.py
│   ├── config.py               # Настройки приложения
│   ├── logger.py               # Логирование
│   └── paths.py                # Работа с путями
│
└── assets/                     # Ресурсы (иконки, шрифты, баннеры)
    └── banner_example.png
```

## Установка

```bash
# 1. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate            # Linux/macOS
venv\Scripts\activate               # Windows

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Установить ffmpeg в систему (требуется для moviepy и whisper)
#    Windows:  https://www.gyan.dev/ffmpeg/builds/
#    macOS:    brew install ffmpeg
#    Linux:    sudo apt install ffmpeg
```

## Запуск

```bash
python main.py
```

## Этапы разработки

- [x] Шаг 1. GUI и загрузка видеофайла (готово)
- [ ] Шаг 2. Извлечение аудиодорожки (заглушка)
- [ ] Шаг 3. Транскрипция через Whisper (заглушка)
- [ ] Шаг 4. Поиск хайповых моментов (заглушка)
- [ ] Шаг 5. Нарезка с blur-фоном (заглушка)
- [ ] Шаг 6. Наложение рекламного баннера (заглушка)
